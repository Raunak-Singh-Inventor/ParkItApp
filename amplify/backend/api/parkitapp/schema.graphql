type messages
  @model
  @aws_cognito_user_pools
  @key(fields: ["id", "insertMessageTime"]) {
  id: ID!
  insertMessageTime: String!
  device_data: DeviceData
}

type DeviceData {
  messageTime: String
  gsr: String
  mic: String
  accelZero: String
  accelOne: String
  accelTwo: String
  client_id: String
}
